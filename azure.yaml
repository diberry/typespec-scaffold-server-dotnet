# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: dina-scaffold-dotnet
metadata:
    template: azd-init@1.14.0
services:
    server:
        project: server
        host: containerapp
        language: dotnet
        docker:
            path: server/Dockerfile
            context: .
